# Ignore Jekyll Liquid placeholders that are resolved at build time.
# These appear as invalid URLs like "{{ '/path' | relative_url }}" during static analysis.
regex:\{\{[^}]+relative_url[^}]*\}\}

# Ignore other Liquid-style placeholders such as {{ '/path' }}
regex:\{\{\s*['"]/[^}]+\}\}
